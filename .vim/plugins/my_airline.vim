" ============================================================================ "
"	Vim airline
" ============================================================================ "
let g:airline#extensions#tabline#enabled = 1	"allow TabLine
"let g:airline_powerline_fonts = 1				"This won't work (i failed to install new fonts to support it)
